ASSIGNMENT NO : 9
Name= Vaishnavi kashinath Nelge Roll no=22
def naive_string_match(text, pattern): n = len(text) m = len(pattern) positions = []
if m == 0:
print("Empty pattern! Matches at all positions.") return list(range(n + 1))
if m > n: return [] # pattern can't be longer than text
for i in range(n - m + 1): match = True for j in range(m):
if text[i + j] != pattern[j]:
match = False break
if match: positions.append(i) return positions
def main(): print("=== Naive String Matching Algorithm ===") text = input("Enter the text: ") pattern = input("Enter the pattern: ") result = naive_string_match(text, pattern)
if result:
print(f"\n✅ Pattern found at indices: {result}") for idx in result: print(f" - text[{idx}:{idx+len(pattern)}] = '{text[idx:idx+len(pattern)]}'")
else: print("\n❌ Pattern not found in the text.")
if __name__ == "__main__":
main()
OUTPUT:
=== Naive String Matching Algorithm ===
Enter the text: abcdtffyjjugyfgabcdskrerabcd Enter the pattern: abcd
✅ Pattern found at indices: [0, 15, 24]
-	text[0:4] = 'abcd'
-	text[15:19] = 'abcd'
-	text[24:28] = 'abcd'
=== Code Execution Successful ===
